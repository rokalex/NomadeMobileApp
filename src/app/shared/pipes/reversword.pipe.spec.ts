import { ReverswordPipe } from './reversword.pipe';

describe('ReverswordPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverswordPipe();
    expect(pipe).toBeTruthy();
  });
});
